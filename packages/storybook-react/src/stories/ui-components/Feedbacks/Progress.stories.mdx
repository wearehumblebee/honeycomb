import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { text } from '@storybook/addon-knobs';
import { Progress } from 'theme-ui';

# Progress

Progress is a component rendering styled inline notifications

https://theme-ui.com/components/progress

<Meta
  title="UI Components / Feedbacks / Progress"
  component={Progress}
  parameters={{ layout: 'centered' }}
/>

<ArgsTable of={Progress} />

You can define variants anywhere in your theme, default style is `theme.styles.progress`

<Canvas>
  <Story name="Default">
    {() => {
      const progress = text('Progress', 50);
      return (
        <Progress value={progress} max={100}>
          {progress}%
        </Progress>
      );
    }}
  </Story>
</Canvas>
